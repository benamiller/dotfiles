# Switch to specific sessions with shortcuts
# Goal is to not be able to tell if mouse is plugged in

bind-key -n M-h switch-client -t "run"
bind-key -n M-t switch-client -t "edit"
bind-key -n M-n switch-client -t "git"
